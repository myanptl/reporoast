# RepoRoast — server-side only (NOT exposed to the browser).
# Get a key at https://console.anthropic.com → set this in Vercel Project → Settings → Environment Variables.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx
